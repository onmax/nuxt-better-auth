---
title: `serverAuth()`
description: How the server Better Auth instance is created.
---

```ts
const auth = serverAuth()
const session = await auth.api.getSession({ headers: event.headers })
```

You rarely need to call it directly unless you’re building custom server logic.

## See also

- Better Auth API concepts: https://www.better-auth.com/docs/concepts/api
