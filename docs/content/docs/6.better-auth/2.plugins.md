---
title: "Plugins"
description: "Better Auth plugins (server-side) with doc links and matching client plugins."
---

Plugins are configured in your `server/auth.config.ts` (via `defineServerAuth`).

<BetterAuthPlugins />

See also: https://www.better-auth.com/docs/concepts/plugins
